<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ZCM.Pages.GroupsPopup"
    BackgroundColor="#80000000">

    <Grid>

        <!-- Backdrop -->
        <BoxView BackgroundColor="Transparent">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnBackdropTapped"/>
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- Popup -->
        <Frame WidthRequest="575"
               MaximumHeightRequest="650"
               Padding="20"
               CornerRadius="18"
               BackgroundColor="#1F1F1F"
               HasShadow="False"
               HorizontalOptions="Center"
               VerticalOptions="Center">

            <VerticalStackLayout Spacing="14">

                <!-- Header -->
                <Grid ColumnDefinitions="*,Auto">
                    <Label Text="Groups (Trust)"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"/>

                    <Button Grid.Column="1"
                            Text="✕"
                            Clicked="OnCloseClicked"
                            BackgroundColor="#2A2A2A"
                            TextColor="White"
                            CornerRadius="10"
                            WidthRequest="36"
                            HeightRequest="36"/>
                </Grid>

                <!-- Description -->
                <Label Text="Enabled groups are trusted. Peers sharing at least one enabled group may communicate."
                       TextColor="#AAAAAA"
                       FontSize="12" />

                <!-- Groups List -->
                <Grid RowDefinitions="*"
                      HeightRequest="400">

                    <CollectionView ItemsSource="{Binding Groups}"
                                    SelectionMode="None">

                        <CollectionView.ItemTemplate>
                            <DataTemplate>

                                <Frame Padding="16"
                                       CornerRadius="16"
                                       BackgroundColor="#2A2A2A"
                                       HasShadow="False">

                                    <VerticalStackLayout Spacing="12">

                                        <!-- HEADER ROW -->
                                        <Grid ColumnSpacing="10">
                                            <Grid.ColumnDefinitions>
                                                <!-- Give name column real guaranteed space -->
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Group Name -->
                                            <Entry Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   TextColor="White"
                                                   Placeholder="Group Name"
                                                   BackgroundColor="#333333"
                                                   HeightRequest="36"
                                                   CornerRadius="8"
                                                   HorizontalOptions="Fill"
                                                   MinimumWidthRequest="140"
                                                   LineBreakMode="NoWrap"/>

                                            <!-- Copy Secret (compact text, full meaning via tooltip) -->
                                            <Button Grid.Column="1"
                                                    Text="Copy"
                                                    ToolTipProperties.Text="Copy Secret"
                                                    SemanticProperties.Hint="Copy Secret"
                                                    Clicked="OnCopySecretClicked"
                                                    BackgroundColor="#333333"
                                                    TextColor="White"
                                                    CornerRadius="8"
                                                    HeightRequest="36"
                                                    Padding="12,0"
                                                    MinimumWidthRequest="64"/>

                                            <!-- Regenerate (compact) -->
                                            <Button Grid.Column="2"
                                                    Text="Regen"
                                                    ToolTipProperties.Text="Regenerate Secret"
                                                    SemanticProperties.Hint="Regenerate Secret"
                                                    Clicked="OnRegenerateSecretClicked"
                                                    BackgroundColor="#333333"
                                                    TextColor="White"
                                                    CornerRadius="8"
                                                    HeightRequest="36"
                                                    Padding="12,0"
                                                    MinimumWidthRequest="72"/>

                                            <!-- Enabled Label -->
                                            <Label Grid.Column="3"
                                                   Text="Enabled"
                                                   VerticalOptions="Center"
                                                   TextColor="#CCCCCC"
                                                   FontSize="12"/>

                                            <!-- Switch -->
                                            <Switch Grid.Column="4"
                                                    IsToggled="{Binding IsEnabled}"
                                                    VerticalOptions="Center"/>
                                        </Grid>

                                        <!-- SECRET ROW -->
                                        <Grid ColumnDefinitions="*,Auto"
                                              ColumnSpacing="8">

                                            <Entry Grid.Column="0"
                                                   Text="{Binding SecretHex}"
                                                   IsPassword="True"
                                                   TextColor="White"
                                                   Placeholder="Group Secret (hex)"
                                                   BackgroundColor="#333333"
                                                   HeightRequest="36"
                                                   HorizontalOptions="Fill"
                                                   LineBreakMode="NoWrap"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   x:Name="SecretEntry"/>

                                            <Button Grid.Column="1"
                                                    Text="Show"
                                                    Clicked="OnToggleSecretClicked"
                                                    BackgroundColor="#333333"
                                                    TextColor="White"
                                                    CornerRadius="8"
                                                    HeightRequest="36"
                                                    Padding="12,0"
                                                    MinimumWidthRequest="72"/>
                                        </Grid>

                                    </VerticalStackLayout>

                                </Frame>

                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>

                </Grid>

                <!-- Add Group -->
                <Button Text="+ Add Group"
                        Clicked="OnAddGroupClicked"
                        BackgroundColor="#27AE60"
                        TextColor="White"
                        CornerRadius="12"/>

            </VerticalStackLayout>

        </Frame>

    </Grid>
</ContentPage>